"""Research tooling that reaches beyond a single scanned file.

The sweep scans one file per skill (SKILL.md). Everything here exists to look
at what a skill actually ships alongside that file, which is where behaviour
diverging from the documentation would live.
"""
